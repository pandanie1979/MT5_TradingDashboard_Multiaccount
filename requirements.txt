# MT5 Trading Dashboard - Python 3.12 dependencies
# Pinned to last proven stable versions; avoid pandas 3.x (CoW breaking changes) and plotly 6.x (API removals)
streamlit==1.45.1
pandas==2.2.3
plotly==5.24.1
streamlit-autorefresh==1.2.1
